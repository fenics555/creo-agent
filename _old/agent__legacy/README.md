# _legacy — УСТАРЕВШЕЕ ДОМА (не удаляем по канону, но из работы убрано)

**Правило дома:** чужое и старое **не удаляем** — переносим сюда. Здесь лежит то, что заменено
новым поколением программ, но может понадобиться как след истории.

Дата выноса: **24.09.2026**.

| Что | Почему устарело | Чем заменено |
|---|---|---|
| `pdf_refresh_batch.py` | массовая перепечатка PDF старого поколения: шла через пары «чертёж↔PDF» и окно в витрине | автономная программа **`creo_pdf`** (`agent\creo_pdf`, класс Ж, JLINK) |
| `pdf_refresh_gui.py` | окно того же старого поколения | окно `creo_pdf_gui.bat` |
| `pdf_refresh_tools.py` | инструмент агента `pdf_refresh` — **дубль**: такое же имя объявлял `pdf_tools.py`, из-за чего реестр инструментов терял один из двух | инструмент `pdf_refresh` в `pdf_tools.py` (через CREOSON) + программа `creo_pdf` для массовой работы |
| `index_rename.html` | старая отдельная витрина переименования (14,6 КБ), ссылалась только сама на себя | инструменты агента `rename_preview` / `rename_model` |
| `ui_variants\` (папка) | чертёж-макеты трёх раскладок окна (`v1_tabs`, `v2_side`, `v3_pult`, `live.js`, `common.css`) — делались, чтобы выбрать вид | **вид переключается в боевом окне** (чипы «◫ вкладки / ▤ меню / ⊞ пульт», личная настройка `ui_layout`); макеты больше не нужны, перенесены 24.09.2026 |
| `harvest_gui_settings.json` | сирота: **нигде в коде не читается** (остаток старой версии окна harvest), внутри была тестовая папка `D:\AI\PROBA` | настоящие корни обхода задаёт `agent\kb_roots.txt` (см. `HARVEST_README.md`) |
| `creo_export_test.bat` | тестовый бат лежал в папке программы и ссылался на пробную папку | приёмку делать из своей урны/`D:\AI\PROBA`, в папке программы только рабочие входы |

**Что при этом убрано из тела агента (24.09.2026):**
- `http_handlers.py` — импорт `pdf_refresh_batch` и эндпоинты `/wiz_pdfrefresh_preview`, `/wiz_pdfrefresh_execute`,
  `/wiz_pdf_preview`, `/wiz_pdf_execute` (всего 53 строки);
- `ui\index.html` — окно `wiz_pdf` и висевшая кнопка «📄 PDF Refresh» (2 строки; кнопка, кстати, стояла
  внутри блока `<style>` — то есть была мёртвой косметикой);
- `ui\app.js` — обработчики `open_pdf_wizard`, `close_pdf`, `wiz_pdf_preview`, `wiz_pdf_execute` (16 строк).

Проверка после правки: `python -m py_compile http_handlers.py` — OK; `python dev\ui_check.py` — ALL GREEN;
`wiz_pdf` и `pdf_refresh_batch` в коде агента не встречаются. Бэкапы: `agent\data\backup\pre_legacy_pdf_*`.

**Как вернуть на минуту (если вдруг понадобится старое окно):** скопировать файл из этой папки назад в
`agent\` и вернуть блоки из бэкапа `pre_legacy_pdf_http_handlers.py`. Но правильнее — доработать `creo_pdf`.
