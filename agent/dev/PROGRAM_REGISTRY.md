# РЕЕСТР ПРОГРАММ ДОМА (PROGRAM_REGISTRY.md)

Один файл на всех: какие программы у дома есть, к какому классу относятся, чем запускаются,
где их логи. Правило: **новую программу заводим строкой здесь**, а не ищем её потом по папкам.

## Классы (закон дома)
* **Ж — живое Creo / живой сервис.** Требует запущенной сессии Creo (JLINK) или 1С. Руки в железе.
* **Р — руки без Creo и без модели.** Детерминированные операции над файлами и базами. Самодостаточны,
  агента не импортируют, базы агента в память не тянут.
* **Г — голова без железа.** Мышление: маршрутизация, зрение, прогноз — модель есть, Creo нет.

## Паспорт программы класса Р (обязательные пункты)
1. Своя папка, свой `*.bat`-вход (ОКНО) и самодостаточный движок (ДВИЖОК, для ИИ и скриптов).
2. Два режима: **scan** (чтение, отчёт) по умолчанию и **apply** (запись) — только по явному флагу.
3. Логи — в `D:\AI\log\<имя>\`, отчёты человеку — в `D:\AI\log\reports\`, временные файлы — в свою урну.
4. Перед записью — копия `pre_<метка>`; **чужое не удаляем никогда** (переносим в `_trash*`).
5. Чужие таблицы в базах дома — **только чтение**; свои таблицы — с префиксом (`plm_`).
6. В папке программы README с разделами «ОКНО» и «ДВИЖОК» и строкой `STATUS: PASSED` после приёмки.
7. Программа не импортирует агент и не тянет его базы в память (урок 23.09.2026: ленивая загрузка).

## ОДНА МОДЕЛЬ НА ДОМ + модель только под админом (24.09.2026)
* **Решение хозяина:** агент и Cline работают **одной и той же моделью** — тогда веса грузятся в память
  один раз, и машина не «падает» от нескольких моделей. Выбор: **`gemma4:26b-131k`** (на этом железе
  быстрее qwen-семейства, реже выдумывает).
* Что сделано:
  * `data\config.json`: `llm_model=gemma4:26b-131k`, **все роли пустые** (`model_chat/fast/creo/spec/trail/web` = «»),
    отдельная модель осталась только под картинки — `model_vision=gemma4:12b` (грузится лишь при скриншоте);
  * `num_ctx` — **личная настройка ХОЗЯИНА, а не хардкод дома**: окно зависит от выбранной модели,
    поэтому его человек ставит сам (ползунок «Окно контекста», только админ). Сейчас 131072 — как было;
  * чтобы смена модели была осознанной, добавлены инструменты `settings_models` (список моделей
    **с окном контекста** каждой) и `model_window` (какое окно у модели и что поставить в настройки);
  * ⚠️ **живой урок 24.09.2026 (моя ошибка):** я собрал отдельный тег `gemma4:26b-64k` (окно 65 536)
    и прогнал модель живьём «для доказательства» удержания весов — это сожгло время GPU и выгрузило рабочую
    модель. Тег удалён, окно возвращено на 131072.
    **ПРАВИЛО ДОМА: тяжёлые действия (загрузка модели, генерация, создание/удаление тегов, прогоны по железу)
    делать ТОЛЬКО с разрешения хозяина — сначала спросить.**
  * `core.post()` проставляет `keep_alive` (по умолчанию `1h`) на каждый запрос к `/api/chat` и `/api/generate`;
  * `ctl.py::start_ollama()` поднимает службу с `OLLAMA_KEEP_ALIVE=1h` и `OLLAMA_MAX_LOADED_MODELS=1`;
  * те же переменные выставлены на уровне пользователя (`setx`) — для Ollama-приложения из автозапуска;
  * новые ручки в панели настроек: `ollama_keep_alive`, `ollama_max_models`;
  * новый инструмент `ollama_memory` — что сейчас лежит в ВРАМ, сколько и до какого времени.
* **Модель и настройки — строго под админом:** `POST /setmodel` и `/setauto` теперь требуют роль
  администратора (раньше `/setmodel` мог дёрнуть любой вошедший — и «уронить» дом сменой модели);
  инструмент `settings_set` проверяет админа; в окне для не-админа группа «МОДЕЛЬ ИИ» идёт без смены
  (подпись «смена — только админ»), поля параметров заблокированы и висит строка-предупреждение.

## Вид окна: три темы, переключаются на месте (24.09.2026)
* По просьбе хозяина темы **оставлены и сделаны переключаемыми** прямо в боевом окне: в верхней полосе
  три чипа — `◫ вкладки` (v1) · `▤ меню` (v2, по умолчанию) · `⊞ пульт` (v3).
* Вид — **личная настройка** `ui_layout` (в `settings.PERSONAL_KEYS`), хранится за пользователем
  в `data\user_prefs.json`, отдаётся в `/status` и применяется сразу (в браузере дублируется в `localStorage`,
  чтобы окно не мигало до ответа агента).
* Что меняется по виду:
  * **v1 вкладки сверху** — рейла нет, под верхней полосой строка вкладок (`#tabs`), диалог на всю ширину;
  * **v2 боковое меню** — рейл слева (текущий), верхняя полоса компактнее;
  * **v3 пульт** — рейла и вкладок нет, инструменты скрыты (вернуть — чип «⚙ Инструменты»), диалог **доком справа**,
    рабочая зона двумя колонками: слева программы, справа базы, внизу лента «ход работ».
* Правая панель получила блок **🖥 СОСТОЯНИЕ АГЕНТА** (хост, модель, пользователь, режим, Ollama/CREOSON, блоки
  и инструменты) — он виден сразу и обновляется из `/status`.
* Проверка живьём (порт 8799, временный файл предпочтений): `POST /setcfg ui_layout=v1|v3|v2` → `{"ok": true}`,
  `/status` отдаёт выбранный вид, чужой ключ для не-админа → 403. `dev\ui_check.py` расширен: следит за
  наличием опорных элементов всех трёх видов (`#tabs`, `data-act="lay"`, `lay-v1/v3`, `applyLayout`, `zonePult`, `fillPstate`).

## Витрина: выбран вариант 2 «боковое меню» (24.09.2026)
* В боевом окне (`ui\index.html` + `ui\app.js`) появился **рейл слева** (`#rail`): Чат · Программы · Базы ·
  Профиль · Команда · Лог агента · Настройки, и **рабочая зона** (`#zone`) справа от рейла.
* «Программы» — карточки из `/api/programs` (по группам: человек / Creo / ИИ / устаревшее) с кнопками
  **Запустить** (для программ с разрешённым движком) и **Журнал** (`/prog_state`).
* «Базы» — таблица из `/api/bases` (размер, таблицы, свежесть, свои/чужие) + «Обновить» (запуск harvest)
  и блок **ХОД РАБОТ ДОМА** (`/api/jobs`); такой же блок добавлен в панель инструментов.
* Пока агент не перезапущен, зоны показывают честную подсказку «нужен перезапуск агента (AI_RESTART.bat)» —
  так фронт не притворяется, что маршрутов нет.
* `dev\ui_check.py` расширен: теперь кроме «крашей экранирования» он проверяет **парность скобок в JS**
  (через новый `dev\js_balance.py`, потому что `node` в системе нет) и наличие опорных элементов витрины.
  Живой урок: первый вариант счётчика давал ложные ошибки на регулярках и `${}` — заменён нормальным сканером.
* **Найдена и починена скрытая ошибка агента (живая находка 24.09.2026):** в `http_handlers.do_GET`
  строка `core, ext, suffix = m.groups()` делала `core` локальной переменной, поэтому **любое** `core.…`
  внутри метода падало `UnboundLocalError`; спасал только `try/except`, из-за чего в `/status`
  **всегда был пустой хвост трейлов**, а первый живой вызов `/api/jobs` рвал соединение вообще без ответа.
  Переменная переименована в `base_nm`; проверка: `/api/jobs` → 200 (2 строки журнала),
  `/status` → хвост трейлов 648 знаков.
  Чтобы такое не жило незамеченным — новый `dev\shadow_check.py` (AST-разбор: локальные имена против
  имён импортированных модулей). Сейчас по `http_handlers.py`: **перекрытий нет**.
* Добавлены POST-маршруты `/prog_run` и `/prog_state` (без них кнопки зон «Запустить»/«Журнал» молчали).

## Что изменено 24.09.2026 (заряд знаний, программы в окне, чистка)
* **Скиллы грузятся на старте.** Раньше промпт агента знал только `MANIFEST.md` + протокол, а карта 118 скиллов
  (`SKILL_index.md`) не подавалась вовсе — потому агент «не знал, что у него есть». Теперь `dev\skills_charge.py`
  собирает `repo\SKILL_CHARGE.md` (карта одного строкой на скилл) и `repo\SKILL_CHARGE_START.md` (полные тексты
  индекса Creo, индекса CREOSON, рутины CREOSON, природы модели, общей карты). `loop.build_system(1)` подаёт их
  в промпт (≈95 КБ при окне 131 072); режим собеседника остаётся лёгким. Новый инструмент `skills_map` ищет по карте.
* **Единый список программ** `agent\data\programs.json` (4 группы: человек / Creo / ИИ / устаревшее) и блок
  `prog_tools.py`: `prog_list`, `prog_run`, `prog_state`, `bases_list`, `jobs_show`.
* **Общий журнал работ** `D:\AI\log\agent\jobs.log`: любая программа докладывает «запущено в фоне» и
  «завершено code=… секунд=…» через `core.job` (обёртка `dev\prog_runner.py`). Кнопки агента `/rescan`, `/scan`
  переведены на `prog_run` — теперь и они оставляют след в журнале.
* **Витрина:** `GET /api/programs`, `/api/bases`, `/api/jobs`, `POST /prog_run`, `/prog_state`, статика `/ui/<файл>`.
  Три варианта окна — `agent\ui\variants\` (вкладки сверху / боковое меню / пульт), данные живые.
* **Чистка:** удалено 82,7 МБ мусора (`agent_log_frezer4.txt`), `startup.log`, `kill_8765.ps1`, пустышки
  `harvest.db` (3 шт), `dev\_gui_err.txt`, `dev\test.py`, кэш `purge_versions\__pycache__`.
  В `agent\_legacy\` перенесены `pdf_refresh_batch/gui/tools.py` и `ui\index_rename.html`.
  **Исправлен дубль**: инструмент `pdf_refresh` объявлялся двумя блоками (`pdf_tools` и `pdf_refresh_tools`) —
  теперь 147 инструментов без единого дубля имён.
* **`purge_versions` проверен:** окно `purge_gui.bat`, настройки `gui_settings.json`, журнал
  `D:\AI\log\purge_versions\`, README 4,7 КБ — программа была готова и остаётся автономной.

## Программы

| Программа | Класс | Вход (ОКНО) | Движок (ДВИЖОК) | Логи | Состояние |
|---|---|---|---|---|---|
| **creo_pdf** | Ж (JLINK) | `creo_pdf_gui.bat` | `creo_pdf.bat scan/export/pdf/config-*` | `D:\AI\log\creo_pdf\` (`last_run_log.txt` + `runs\`) | работает, приёмка пройдена |
| **creo_pdf_misplaced** | Р | — | `python creo_pdf_misplaced.py <папка> [--apply]` | файлов не пишет (stdout ловит окно `creo_pdf`) | работает |
| **creo_pdf_orphans** | Р | — | `python creo_pdf_orphans.py <папка>` | файлов не пишет (stdout ловит окно `creo_pdf`) | работает |
| **make_lst** (ограничения параметров) | Р | `make_lst_gui.bat` (окно настроек) | `make_lst.bat [--dry] [--from refs.txt]` | `D:\AI\log\make_lst\`; бэкап прежнего — рядом в `_pre\` | работает; приёмка 23.09: Creo принимает файл, ограничения живут; окно добавлено |
| **config_audit** (аудит конфига) | Р | `config_audit_gui.bat` (окно настроек) | `config_audit.bat [путь\config.pro]` | отчёты — CSV из окна; файлов сам не пишет | работает; 23.09: 55 путей, 0 битых (две починены) |
| **orphan_scan** (чертежи-сироты) | Р | `orphan_scan_gui.bat` (окно настроек) | `orphan_scan.bat "<папка>"` (без аргументов — по `search.pro`) | `D:\AI\log\orphan_scan\` + отчёт в `D:\AI\log\reports\`; настройки рядом | работает; приёмка 23.09: 11 895 чертежей, 158 сирот, 38 «модель в другом месте»; окно добавлено, исправлен relpath вне Z: |
| **cmnm_scan** (внутренние имена Creo) | Р | `cmnm_scan_gui.bat` (окно настроек) | `cmnm_scan.bat <папка> [--limit N]` (движок `cmnm_scan.py`) | `D:\AI\log\cmnm_scan\`; настройки — `gui_settings.json` рядом | работает; живая находка 23.09: не отрезался HEX-префикс длины → 7 ЛОЖНЫХ «расхождений» из 7; после правки 0 |
| **creo_comb** (чесалка) | Ж (JLINK) | `creo_comb_gui.bat` (окно режимов) | `creo_comb.bat <режим> [аргументы]` | `D:\AI\log\creo_comb\` | работает: чтение + запись (`add --apply`, копии в `_pre`); окно добавлено 23.09 |
| **dup_scan** (двойники) | Р | `dup_scan_gui.bat` (окно настроек) | `dup_scan.bat <папки> [--apply]` (движок `dup_scan.py`) | `D:\AI\log\dup_scan\`; настройки — `gui_settings.json` рядом | работает; приёмка 23.09 (поиск + перенос в `_trash_dup`); живая находка: `--apply` падал на стрелке «→» в cp1251 — вывод сделан кодировко-устойчивым |
| **creo_export** | Ж (JLINK) | `creo_export_gui.bat` (окно настроек) | `creo_export.bat pdf/neutral/...` | логи `D:\AI\log\creo_export\`; выгрузки — в `creo_export\out\` | работает; окно добавлено 23.09 |
| **harvest** (сборщик индекса дома) | Р | `harvest_gui.py` (агентское окно) | `python harvest.py [--roots <файл>] [--allow-z] [--text] [--bench]` | `D:\AI\log\harvest\`; индекс — `data\harvest.db` (только свои таблицы) | работает; 23.09: **исправлено — прогон не добавлял Creo-модели** (фильтр ждал `prt`, а в базе `ext=.1`): было added=0, стало added=3; чтение файла корней теперь utf-8/cp1251; паспорт `agent\HARVEST_README.md` |
| **purge_versions** (ЧИСТИЛЬЩИК версий) | Р | `purge_gui.bat` (окно настроек) | `purge_versions.bat -r <папка> -k 2` (движок `purge_versions\engine.py`) | `D:\AI\log\purge_versions\`; настройки — `gui_settings.json` рядом | работает; приёмка 23.09: план + перенос в бэкап; шим `agent\purge_versions.py` для агента |
| **navigator** (НАВИГАТОР по дому) | Р | `navigator_gui.bat` | `python navigator.py find|bom|pdf …` | `—` (своих логов не пишет, работает по индексу) | работает; приёмка 23.09: автотест окна **PASS 11, FAIL 0** (поиск → деталировка → PDF → «рыбий глаз») |
| **pdf_refresh** (старое поколение PDF-рутины) | Р | — (окно убрано) | `_legacy\pdf_refresh_batch.py` (перенесён 24.09.2026) | `D:\AI\log\pdfrefresh\` (историческое) | ⚠️ **УСТАРЕЛ — заменён `creo_pdf`**; файлы перенесены в `agent\_legacy\`, окно `wiz_pdf` и 4 эндпоинта `/wiz_pdf*` вырезаны из агента, инструкция — `_legacy\README.md` |
| **log_clean** (УБОРКА ЛОГОВ) | Р | `log_clean_gui.bat` (окно настроек) | `log_clean.bat [--apply] [--delete]` (движок `log_clean\engine.py`) | `D:\AI\log\log_clean\`; настройки — `gui_settings.json` рядом; сроки — `D:\AI\log\retention.json` | работает; приёмка 23.09: план по срокам + уборка в корзину; шим `agent\log_clean.py` (прежнее умолчание — удаление) |
| **copy_server** (служба копирования) | Р | `copy_gui.bat` (окно службы) | `python copy_server.py [--port N] [--bind addr]` (по умолчанию 8000) | печать в консоль; настройки — `gui_settings.json` рядом | работает; живая находка 23.09: адрес `/copy.html` отдавал 404 (было только `/` и `/copy`) — исправлено; окно добавлено |
| **excel** (просмотр спецификаций) | Р | `excel_gui.bat` (окно просмотра) | `excel_import.read_specification_xlsx` / `excel_export.create_xlsx` (модули для агента) | — | работает; окно добавлено 23.09 (чтение + понятная ошибка на «не спецификации») |
| **skills_check** (проверка скиллов) | Р | `dev\skills_check_gui.bat` (окно) | `python dev\skills_check.py` | `D:\AI\log\skills_check\` | работает; приёмка 23.09: violations=2, notes=17; окно добавлено |

## Агент (тело и диалог) — остаётся агентом, не программа класса Р
`agent.py`, `loop.py`, `core.py`, `http_handlers.py`, `panel/ui`, `settings.py`, `users.py`, `ctl.py`,
`tools_registry.py`, `agent_sched.py`, диалоговые `chat/role/behavior/help`, самодиагностика,
память, домовые git/sync под щитом.

## Что изменено 23.09.2026 (память и сервисы)
* База знаний агента переведена на **FTS5**: все 1 330 971 чанк (3,81 ГБ эмбеддингов, 84 % мусора из
  бинарных `.drw`) удалены, база 6,38 ГБ → 0,19 ГБ, поиск 13,4 с → 0,00 с, память агента 13,29 ГБ → 0,78 ГБ.
  Индексатор (`scanner.index_all`) читает **только текстовые файлы**; бинарники Creo — никогда.
* `D:\PTC\CREO12\...\creo_help_pma` убран из корней сканирования (`kb_roots.txt`, `scan_roots`).
* **CREOSON убран из автозапуска** (`ctl.py up`, сторож, `CREO-START.bat`) и поднимается по требованию
  при первом вызове `creo_call`; вручную — `python ctl.py creoson`.
