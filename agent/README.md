# Агент — локальный инженерный помощник (порт 8765)

**Рантайм:** `D:\AI\tools\agent` · **Порт:** 8765 · **Обновлено:** 24.09.2026

**Состав:** тонкий вход `agent.py`; голова и диалог `loop.py`; тело HTTP и витрина
`http_handlers.py`; ночной цикл и сторож `sched.py`; ядро `core.py`; настройки `settings.py`;
реестр `tools_registry.py` (автоподключение блоков `*_tools.py`); сканер `scanner.py`
(индекс памяти и предохранитель чистки); PDF-инструменты `pdf_tools.py`; пользователи `users.py`
(токены, администратор из секретов); управление `ctl.py` (идемпотентный подъём стека,
сторож с дедупликацией по pid-файлу).

## Как работать с домом
- **Управление:** `python ctl.py up [--browser] [--hidden]` — подъём стека
  (Ollama 11434, CREOSON 8080, copy-server 8000, агент 8765).
  Статус: `python ctl.py status` · сторож: `python ctl.py --watch` ·
  стоп: `python ctl.py down` · рестарт: `python ctl.py restart`.
- **Правки интерфейса:** после изменений в `ui/` прогнать `python dev/ui_check.py`
  и `python dev/ui_probe.py`.
- **Стоп ответа:** пока агент «думает», в окне есть кнопка **■ СТОП** (и клавиша Esc) — она рвёт
  и запрос из окна, и саму генерацию модели (`/ask_cancel`), недоделанный обмен в историю не пишется.
  Проверка: `python dev\stop_button_check.py`.
- **Бекапы:** перед каждой правкой — копия `pre_<метка>_<файл>` в `data\backup\`.
- **Секреты:** только `data\secrets.json`, вне git.

## Автономные программы дома
Полная карта с разбором автономности: **`D:\AI\tools\README.md`**. Коротко:
- **без Creo:** `dup_scan`, `cmnm_scan`, `make_lst`, `config_audit`, `orphan_scan` (нужны базы дома),
  `log_clean`, `purge_versions`, `copy\copy_server`, `dev\skills_check.py`, `excel\*`;
- **нужен живой Creo (JLINK, без CREOSON):** `creo_pdf`, `creo_export`, `creo_comb`;
- у каждой автономной программы **свой `README.md`** в её папке (образец — `creo_pdf\README.md`).
Автономная = запускается без агента, всё своё в своей папке, логи в `D:\AI\log\<имя>\`.

## Принципы
- **Руки без ИИ.** Детерминированные операции (скан и индекс, пары PDF/чертёж, чистка версий,
  уборка логов) работают скриптами, планировщиком и кнопками витрины — без обращения к модели.
- **ИИ рядом с руками.** Модель подсказывает инструмент, контролирует результат против плана,
  разбирает неудачи; пишущие операции — только под щитом согласования.
- **Деградация без пустышки.** При недоступной модели прямые вызовы, поиск и панели продолжают работу.

## История
- 14.09.2026 — восстановление модулей после периода роста, обновление паспорта и ридми.
- 17.09.2026 — аудит правил дома, крах-скиллы, манифест в системный промпт.
- 21.09.2026 — голова, ночь и HTTP вынесены в отдельные модули; ридми приведён к нейтральному виду.
- 24.09.2026 — кнопка «■ СТОП» (обрыв генерации, `/ask_cancel`), переключаемые виды окна,
  единый список автономных программ (`data\programs.json`), устранено мерцание окон консоли
  (сторож и синхронизация — без видимых окон).

## Ссылки
- Паспорт дома: `D:\AI\repo\PASSPORT.md` · Карта скиллов: `D:\AI\repo\SKILL_index.md`
- Правила: `D:\AI\repo\MANIFEST.md` и `D:\AI\.clinerules`

## Лицензия
MIT
