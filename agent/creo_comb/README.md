# ЧЕСАЛКА (creo_comb) — JLINK, без CREOSON

**Класс Ж** (живое Creo): причёсывает детали и сборки по ЭТАЛОНУ — шаблонам, прописанным
в `config.pro`. Сейчас готово только **чтение** (пробы и отчёт), запись (`add`) — следующим шагом.

## ОКНО (для рук)
```
creo_comb.bat tpl-plan                      — какие шаблоны прописаны в конфиге и есть ли файлы (без Creo)
creo_comb.bat refs                          — уравнения и параметры ЭТАЛОНОВ (нужен Creo)
creo_comb.bat dump <папка|файл> [имя]        — уравнения+параметры одной модели
creo_comb.bat scan <папка> [config.pro]      — чего не хватает моделям папки против эталонов
```

## ДВИЖОК (для ИИ и скриптов)
`creo_comb.bat <режим> [аргументы]` — все режимы пишут отчёт в консоль (перенаправляется в файл).
Запись в модели — только режим `add` и только с флагом `--apply`.

## РЕЖИМЫ
```
tpl-plan [config.pro]              — шаблоны из конфига: путь, файл, версия (без Creo)
refs                               — уравнения и параметры ЭТАЛОНОВ (нужен Creo)
dump <папка|файл> [имя]            — уравнения+параметры одной модели
scan <папка> [config.pro]          — чего не хватает моделям папки против эталонов (только чтение)
scan-here [config.pro]             — то же по текущей папке сессии (пути без кириллицы в аргументах)
add <папка> [config.pro] [--apply] — ДОБАВИТЬ недостающие параметры и уравнения
probe-open <папка> <имя>           — проба открытия: почему Creo не берёт чертёж
probe-open-f <файл-со-списком>     — то же по списку «<папка>|<имя>» (обход проблем с кириллицей в cmd)
```

## ЧТО ДЕЛАЕТ `add`
1. Читает ЭТАЛОНЫ из `config.pro` (`template_solidpart`, `template_designasm`) — параметры, уравнения,
   уравнения пострегенерации и **ОГРАНИЧЕНИЯ** (перечислимые значения: ТИП, ТИП2, ФОРМАТ,
   РОЛЬ_В_ЛИТЕЙНОЙ_СИСТЕМЕ, материалы).
2. Для каждой модели папки считает **недостающее** (по именам; ничего не заменяет).
3. Без `--apply` — только план. С `--apply` — по каждой модели:
   копия `<папка>\_pre\<дата_время>_<файл>` → создание параметров (значение из эталона, пустое — пустым,
   `ТИП` = Деталь/Сборка по типу модели) → уравнения и пострегенерация ДОПИСЫВАЮТСЯ к существующим →
   регенерация → `Save()`.
4. Итог: `проверено N, к правке M, исправлено K, ошибок E`.

## Приёмка (23.09.2026, песочинка `D:\AI\PROBA\combtest`)
- контроль: 3 причёсанные модели (`калибр.prt.1`, `проверка.prt.1`, `ыва.asm.1`) → «к правке 0» ✓
- `p-515-300_008am-03.prt.1` (14 параметров отсутствовало) → план: `+параметров 14` ✓
- `--apply` → `ИСПРАВЛЕНО: параметров +14`, копия `_pre\20260923_170250_…prt.1`, файл сохранён **новой
  версией** `…prt.2` (383,2 КБ) ✓
- повторный план → `к правке 0, ошибок 0` (идемпотентно) ✓
- ограничения эталона прочитаны: ТИП, ТИП2, ФОРМАТ, РОЛЬ_В_ЛИТЕЙНОЙ_СИСТЕМЕ, PTC_*MATERIAL ✓
  (в шаблоне сборки в списке РОЛЬ_В_ЛИТЕЙНОЙ_СИСТЕМЕ — опечатка `Моделль`, надо исправить в шаблоне)

## ТИП и ПРОИЗВОДСТВО (уточнено пробами 23.09.2026)
* `ТИП` — ограниченный параметр: **деталь → `Деталь`**, **обычная сборка → `Сборка`**,
  **производственная модель → `Производство`** (она выглядит как сборка!).
* **Неоспоримый признак производства — расширение `.mfg`** (в дереве дома 414 таких файлов).
  `pfcMFG.MFG` в Java и `ModelType` этого не различают (проба `mfgcheck`: `.mfg` → тип `1`, объект MFG = false);
  в C-API для этого есть `ProMdlSubtypeGet` (`PROMDLSTYPE_MFG_NCASM` и др.).
* Маркеры дома (`ТИП = Производство`, параметр `ПАРТИЯ`) — **подсказка, а не доказательство**: у таких
  `.asm` тип сборки и признаков MFG нет, это **оснастка**. Поэтому:
  - `add` **не открывает `.mfg`** и **не меняет ТИП** у оснастки с маркерами дома (в производство пока не лезем);
  - `typcheck` показывает их отдельной строкой `производственная/оснастка … — не трогаем`.
* Режимы для аудита: `typcheck <папка>` (ТИП против природы модели) и `mfgcheck <папка>`
  (таблица: тип, объект MFG, `.mfg`, писаные ТИП/ПАРТИЯ — для разбора, что есть факт, а что — данные дома).
* Изученный производственный набор (`d25`): `d25.mfg` + `d25.asm` + `d25.prt` + **`d25z.prt` (заготовка)** +
  **`platina.prt` (приспособление)** + `sbsb.asm`. Скилл: `Creo\STANDARDS\SKILL_creo_model_nature.md`.

## ОГРАНИЧЕНИЯ ПАРАМЕТРОВ (варианты) — проверено 23.09.2026
* В `config.pro` включён `restricted_val_definition` → `НАСТРОЙКИ\ФАЙЛЫ_ОГРАНИЧЕНИЙ_ПАРАМЕТРОВ\list.lst`
  (файл собран из **ограничений эталонов**; прежний файл — в `START-STD\БЕКАП\2026-09-23_1709_pre_list.lst`).
* Ограниченные параметры дома: `ТИП`, `ТИП2`, `ФОРМАТ`, `РОЛЬ_В_ЛИТЕЙНОЙ_СИСТЕМЕ` (+ `GROUP`).
  `PTC_MATERIAL_NAME`/`PTC_MASTER_MATERIAL` в файл НЕ внесены: в шаблоне они ограничены одним материалом,
  а в доме есть и другие (STAL_45) — глобальное ограничение их бы заблокировало.
* **Creo не даёт создать ограниченный параметр с ПУСТЫМ значением через API** (`XToolkitNotValid`):
  пустой вариант (`' '`) Creo вырезает из списка при создании параметра. Поэтому:
  - по умолчанию чесалка такие параметры **пропускает и докладывает**
    («ПРОПУЩЕНЫ (ограничены, пустое значение Creo через API не даёт…)»);
  - с флагом `--empty-first` ставит **первое значение из списка** (например `Литниковая_система`).
* Ограничение, УЖЕ записанное в модели, главнее файла: если у модели свой список (например
  `ТИП → [ , Деталь, Прочие, Сборка, Стандарт]`), Creo применяет именно его.
* В шаблоне сборки в списке `РОЛЬ_В_ЛИТЕЙНОЙ_СИСТЕМЕ` есть опечатка **`Моделль`** (в детали — `Модель`);
  правится только руками в диалоге «Опции детализации/Параметры» (API на запись ограничений не умеет).

## Правила дома, уже вшитые
* **Эталон — из config.pro**, а не из головы: `template_solidpart`, `template_designasm`,
  `template_sheetmetalpart`. Creo-версии разрешаются: `mm_part.prt` → `mm_part.prt.1`.
* **Читаем оба вида уравнений**: обычные (`GetRelations`) и **пострегенерации**
  (`GetPostRegenerationRelations`) — там живёт `MASS = PRO_MP_MASS`.
* Рабочий каталог сессии сохраняется и возвращается на место; открытые модели убираются из сессии.
* Если живых сессий Creo несколько, JLINK даёт `XToolkitAmbiguous` — правило дома: **одна сессия Creo**.

## Приёмка (23.09.2026)
* `tpl-plan` на боевом config.pro: `mm_part.prt.1`, `sborka_mm.asm.1`, `mm_sheet.prt.1`,
  `pressforma-mm_mold.asm.1`, `komponovka_form_mm_cast.asm.1`, `sborka_mfg.asm.1` — файлы есть;
  **`template_mold_layout` (`mm_mold_lay.asm`) — файла нет (битая настройка)**;
  `template_drawing`, `template_boardpart`, `template_new_ecadasm` — через `$PRO_DIRECTORY\ШАБЛОНЫ[s]`
  (каталога нет, в `ШАБЛОНЫs` опечатка).
* `refs`/`scan` на живой сессии — в работе (сессия вышла во время пробы).
* STATUS: чтение — READY, запись (`add`) — НЕ ГОТОВО.
